[project]
name = "points-mall-data"
version = "0.1.0"
description = "Points Mall Data Service (FastAPI)"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.110.0",
    "uvicorn[standard]>=0.27.0",
]

[tool.uvicorn]
host = "0.0.0.0"
port = 8001
reload = true
